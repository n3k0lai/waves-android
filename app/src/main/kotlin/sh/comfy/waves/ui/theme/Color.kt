package sh.comfy.waves.ui.theme

import androidx.compose.ui.graphics.Color

// Waves palette — deep teal ocean → sandy gold → white foam → void black

// Primary: deep teal ocean
val WavesPrimary = Color(0xFF00897B)
val WavesPrimaryDark = Color(0xFF00695C)
val WavesPrimaryLight = Color(0xFF4DB6AC)
val WavesOnPrimary = Color(0xFFFFFFFF)

// Secondary: sandy gold shoreline
val WavesSecondary = Color(0xFFD4A574)
val WavesSecondaryDark = Color(0xFFA1785A)
val WavesOnSecondary = Color(0xFF1A1A1A)

// Tertiary: white foam
val WavesTertiary = Color(0xFFE0F2F1)
val WavesOnTertiary = Color(0xFF1A1A1A)

// Surface: void black
val WavesSurface = Color(0xFF0A0A0A)
val WavesSurfaceVariant = Color(0xFF1A1A1A)
val WavesOnSurface = Color(0xFFE0E0E0)
val WavesOnSurfaceVariant = Color(0xFF9E9E9E)

// Background
val WavesBackground = Color(0xFF050505)
val WavesOnBackground = Color(0xFFE0E0E0)

// Error
val WavesError = Color(0xFFCF6679)
val WavesOnError = Color(0xFF000000)

// Outline
val WavesOutline = Color(0xFF2A2A2A)
val WavesOutlineVariant = Color(0xFF1E1E1E)

// Ene accent
val WavesEneAccent = Color(0xFF00D4AA)
